\chapter{基于自适应不确定性度量的离线强化学习算法}\label{Chap:chap3}

本章详细介绍本文提出的基于自适应不确定性度量的离线强化学习算法。首先分析现有基于模型的离线强化学习方法在不确定性估计与约束方式上的局限，并据此引出本文的关键设计；随后按照“模型学习—不确定性度量—奖励重塑—权重调节”的顺序阐述完整算法；接着给出理论分析，说明累积不确定性度量与自适应权重的合理性；最后在D4RL基准测试集上进行实验验证并与现有方法对比。


\section{问题分析与算法动机}\label{Sec3.1}

基于模型的离线强化学习通过在离线数据上学习环境转移模型，并借助模型推演生成虚拟交互数据来优化策略，具有较高的样本效率。然而，离线数据覆盖有限会导致模型在分布外区域的预测偏差显著。为缓解由模型偏差带来的性能退化，以MOPO\cite{Yu2020MOPO}为代表的一类方法会基于模型不确定性构造惩罚项，对模型推演得到的回报进行保守修正，从而倾向于在模型更可靠的区域内更新策略。

这类方法的约束信号通常来自单步转移不确定性（例如预测方差或集成分歧），更侧重“当前一步是否可靠”，而较少显式刻画“到达当前状态的推演路径是否可靠”。由于离线强化学习本质上是序列决策过程，模型误差会在多步推演中传播并可能被放大：即使某些时刻的单步转移不确定性较低，若该状态由高风险推演路径到达，后续推演仍更容易偏离离线数据覆盖范围，从而形成累积风险。

此外，现有方法中的惩罚强度通常设置为常数或依赖人工调参，缺少随训练阶段自适应调节的机制。随着策略迭代更新，其在模型中访问的状态--动作分布会持续变化：训练早期若惩罚过强，策略更新可能过于保守而难以充分利用模型生成数据；训练后期若惩罚过弱，则更易出现分布外推演导致的性能退化。因此，固定惩罚系数难以兼顾不同阶段的需求。

基于以上分析，本章首先引入累积状态不确定性度量，通过综合考虑从初始状态到当前状态的历史不确定性累积，使智能体更全面地评估决策风险；基于以上的度量，进一步设计自适应动态权重机制，根据策略优化进程动态调节惩罚强度，在训练早期适当减弱惩罚以充分利用模型生成数据，在后期逐步增强惩罚以抑制分布外推演风险，从而实现对"性能提升—风险约束"权衡的自适应调节。


\section{算法框架}\label{Sec3.2}

基于第\ref{Sec3.1}节的分析，本节概述本章方法的整体流程，并说明各模块之间的依赖关系。整体流程包括三步：首先，基于离线数据学习概率转移模型；再基于此模型生成短期虚拟轨迹，同时刻画单步转移不确定性与沿轨迹传播的累积状态不确定性；最后通过随训练进程变化的惩罚权重进行奖励重塑，并在SAC框架下更新策略。算法整体流程如图\ref{Fig:Chap3_framework}所示。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.9\textwidth]{figures/framework.pdf}
  \caption{基于自适应不确定性度量的离线强化学习算法框架。第一阶段基于离线数据集训练概率转移模型；第二阶段利用转移模型生成虚拟轨迹，同时计算累积状态不确定性和状态转移不确定性，通过自适应权重进行奖励重塑，最终基于SAC算法优化策略。}
  \label{Fig:Chap3_framework}
\end{figure}


\subsection{概率转移模型构建}\label{Sec3.2.1}

在基于模型的离线强化学习中，首先需要从离线数据集$\mathcal{D}_{off} = \{(s_i, a_i, s_i', r_i)\}_{i=1}^N$中学习环境的状态转移函数。本文采用神经网络参数化的概率转移模型$\hat{T}_\theta(s', r|s, a)$，以当前状态$s$和动作$a$为输入，输出下一状态$s'$和奖励$r$的条件分布。

转移模型采用高斯分布进行建模：
\begin{equation}\label{Eq:Chap3_transition_model}
  \hat{T}_\theta(s', r|s, a) = \mathcal{N}\left(\mu_\theta(s, a), \Sigma_\theta(s, a)\right),
\end{equation}
其中$\mu_\theta(s, a) \in \mathbb{R}^{d_s + 1}$为预测均值向量（包含下一状态和奖励），$\Sigma_\theta(s, a) \in \mathbb{R}^{(d_s+1) \times (d_s+1)}$为预测协方差矩阵，$d_s$为状态空间维度。为简化计算，本文采用对角协方差矩阵。

模型网络采用多层感知机结构，包含3个隐藏层，每层256个神经元，使用ReLU激活函数。输出层分为两个分支：均值分支直接输出预测均值，方差分支输出对数方差，通过指数变换得到正定的方差估计，保证数值稳定性。

模型参数通过最大化离线数据的对数似然进行优化，训练损失定义为：
\begin{equation}\label{Eq:Chap3_model_loss}
  \mathcal{L}(\theta) = -\mathbb{E}_{(s, a, r, s') \sim \mathcal{D}_{off}}\left[\log \hat{T}_\theta(s', r|s, a)\right],
\end{equation}
展开高斯分布的对数似然，训练损失可写为：
\begin{equation}\label{Eq:Chap3_model_loss_expand}
  \mathcal{L}(\theta) = \mathbb{E}_{(s, a, r, s') \sim \mathcal{D}_{off}}\left[\frac{1}{2}(y - \mu_\theta)^T \Sigma_\theta^{-1} (y - \mu_\theta) + \frac{1}{2}\log|\Sigma_\theta|\right],
\end{equation}
其中$y = [s'; r]$为真实的下一状态和奖励拼接向量。该损失函数同时优化预测均值和预测方差：均值项最小化预测误差，方差项约束模型输出合理的不确定性估计。


\subsection{基于不确定性惩罚的策略优化}\label{Sec3.2.2}

完成转移模型训练后，策略优化阶段利用转移模型生成虚拟轨迹，并基于不确定性惩罚进行策略更新。与MOPO\cite{Yu2020MOPO}类似，本文定义状态转移不确定性为模型预测方差的范数：
\begin{equation}\label{Eq:Chap3_transition_uncertainty}
  u_{trans}(s, a) = \|\Sigma_\theta(s, a)\|_F,
\end{equation}
其中$\|\cdot\|_F$表示Frobenius范数。该度量反映了模型对状态转移预测的置信程度：在数据覆盖充分的区域，模型预测方差较小；在数据稀疏的分布外区域，模型预测方差较大。

策略优化采用Soft Actor-Critic（SAC）\cite{Haarnoja2018SAC}算法框架。SAC维护策略网络$\pi_\varphi$和两个Q值网络$Q_{\theta_1}, Q_{\theta_2}$，通过最大化带熵正则的期望回报优化策略：
\begin{equation}\label{Eq:Chap3_sac_objective}
  J(\varphi) = \mathbb{E}_{s \sim \mathcal{D}, a \sim \pi_\varphi}[Q_\theta(s, a) - \alpha_{ent} \log \pi_\varphi(a|s)],
\end{equation}
其中$\alpha_{ent}$为温度参数，控制探索与利用的平衡。训练数据从离线数据集$\mathcal{D}_{off}$和模型生成数据集$\mathcal{D}_{model}$中按比例采样。模型生成数据通过从离线数据集中采样初始状态，利用当前策略与转移模型交互生成短期轨迹获得。

上述策略优化以单步转移不确定性为主要约束信号，但在模型推演中，不确定性会沿轨迹传播并累积：即使每一步的预测方差较小，长步数推演仍可能将状态推至离线数据覆盖之外。为使策略能够识别“由高风险推演路径到达的状态”，下文引入累积状态不确定性，并将其与转移不确定性共同用于奖励重塑。

\subsection{累积状态不确定性与奖励重塑}\label{Sec3.2.3}


为解决仅考虑状态转移不确定性的局限，本文引入累积状态不确定性的概念：状态$s_t$的不确定性不仅取决于从$s_{t-1}$转移到$s_t$的不确定性，还取决于从初始状态到$s_{t-1}$的累积不确定性。这一设计的核心思想是：强化学习是序列决策过程，不确定性会随轨迹传播和累积。

定义状态$s_t$的累积不确定性为：
\begin{equation}\label{Eq:Chap3_cumulative_uncertainty}
  u(s_t) = \mathbb{E}\left[\sum_{t'=0}^{t} \omega^{t-t'} \log\left(1 + u_{trans}(s_{t'}, a_{t'})\right)\right],
\end{equation}
其中$\omega \in (0, 1]$为折扣因子。该定义的核心思想是：状态$s_t$的不确定性由从初始状态到当前状态的所有状态转移不确定性的折扣和构成。引入折扣因子$\omega$的原因在于：（1）从不同的初始状态都有可能到达$s_t$，因此应给时序上更近的状态赋予更高的权重；（2）早期状态转移的不确定性对当前状态的影响随时间递减。取对数是为了使不确定性度量更加平滑，避免数值不稳定。

需要说明的是，上式包含对轨迹分布的期望。为使“状态到不确定性”的映射便于后续进行函数逼近，本文将$u(s)$理解为累积量$u_t:=\sum_{t'=0}^{t} \omega^{t-t'} \log\left(1 + u_{trans}(s_{t'}, a_{t'})\right)$在给定当前状态$s_t=s$条件下的条件期望，即$u(s)=\mathbb{E}[u_t\mid s_t=s]$。在实现中，$u(s)$通过在转移模型中采样得到的短期推演轨迹进行蒙特卡洛估计。

基于上述定义，累积状态不确定性可通过递推方式进行高效更新：
\begin{equation}\label{Eq:Chap3_uncertainty_update}
  u(s') = \omega \cdot u(s) + \log\left(1 + u_{trans}(s, a)\right),
\end{equation}
其中$s'$为从状态$s$执行动作$a$后转移到的下一状态。该递推公式表明：下一状态的累积不确定性由当前状态的累积不确定性（经折扣后）与当前状态转移的不确定性之和构成。相比重新计算完整历史，递推更新的时间复杂度为$O(1)$，适合在线应用。

在本文的离线设定中，上述递推主要用于转移模型推演过程中对生成轨迹进行实时标注，从而为后续的不确定性预测网络提供监督信号。

在实际应用中，由于状态空间通常是连续的，难以显式维护每个状态的累积不确定性。更重要的是，累积不确定性沿轨迹递推更新，其数值在形式上依赖于历史信息。为此，本文引入一个额外的神经网络$u_\phi(s)$来学习状态到“期望累积不确定性”$u(s)=\mathbb{E}[u_t\mid s_t=s]$的映射关系，使得在后续的策略更新中无需追踪完整历史即可对任意访问状态给出一致的风险评估。

不确定性预测网络采用多层感知机结构，包含2个隐藏层，每层128个神经元，使用ReLU激活函数，输出为标量的不确定性估计值。网络的训练采用监督学习方式。在智能体与环境模型交互生成虚拟轨迹的过程中，对每个访问的状态$s$，根据公式(\ref{Eq:Chap3_uncertainty_update})计算其累积不确定性$u(s)$，并将$(s, u(s))$对存入经验回放池$\mathcal{D}_{model}$。在训练过程中，从经验回放池中采样批次数据，通过最小化均方误差损失优化网络参数：
\begin{equation}\label{Eq:Chap3_uncertainty_loss}
  \mathcal{L}(\phi) = \mathbb{E}_{(s, u(s)) \sim \mathcal{D}_{model}}\left[\left(u_\phi(s) - u(s)\right)^2\right].
\end{equation}

通过该网络，智能体可以快速评估任意状态的累积不确定性，无需在线计算完整的历史轨迹。在推理时，网络的前向传播复杂度为$O(d_\phi)$，其中$d_\phi$为网络参数量，相比存储和查询完整历史显著降低了计算开销。



基于累积状态不确定性和状态转移不确定性，本文提出综合的奖励重塑机制。当智能体处于状态$s$并执行动作$a$时，通过环境模型获取下一状态$s'$和奖励$r$，并计算状态转移不确定性度量$u_{trans}(s,a)=\|\Sigma_\theta(s,a)\|_F$以及下一状态的累积不确定性$u_\phi(s')$。为与累积不确定性定义中采用的对数平滑保持一致，本文对即时不确定性项同样采用$\log(1+u_{trans}(s,a))$进行尺度压缩与数值稳定化。重塑后的奖励定义为：
\begin{equation}\label{Eq:Chap3_reward_shaping}
  \tilde{r} = r - \lambda \cdot \left(\alpha \cdot u_\phi(s') + \log\left(1 + u_{trans}(s, a)\right)\right),
\end{equation}
其中$\lambda$为不确定性惩罚的总体权重，$\alpha$为平衡状态不确定性与转移不确定性的超参数。

该机制综合考虑了状态不确定性和转移不确定性，不仅惩罚高不确定性的状态转移，还惩罚进入高不确定性状态的行为，使智能体能够更全面地规避分布外区域。累积不确定性能够捕捉历史信息的影响——即使某一步的即时不确定性较低，如果该状态通常是通过多次高不确定性转移到达的，其累积不确定性仍然较高，会受到相应的惩罚。此外，通过超参数$\alpha$可以灵活调节两类不确定性的相对权重，适应不同任务的特点。

奖励重塑将不确定性显式引入策略更新，但其强度不宜固定不变：训练早期需要一定的探索自由度以避免过早陷入局部最优，而训练后期更需要稳健的保守性以减少分布外风险。基于这一阶段性差异，下文给出自适应动态权重机制，用于随训练进程平滑调节惩罚强度。

\subsection{自适应动态权重机制}\label{Sec3.2.4}



仅通过累积不确定性度量进行奖励重塑，智能体仍可能陷入过于保守的情况，无法充分探索环境以优化策略。这是因为策略优化的不同阶段对探索和保守的需求存在根本差异。

在训练初期，智能体的策略尚未稳定，需要充分探索环境模型中的各个状态以收集多样化经验。此时若施加过强的不确定性惩罚，智能体将被限制在离线数据密集覆盖的狭隘区域，难以发现潜在的高价值策略。相反，适度的探索自由度对于策略初期的学习至关重要。

在训练后期，智能体的策略逐渐收敛，应减少对不确定状态的探索，增加保守性以确保策略的稳健性。过度的探索可能导致策略在收敛阶段产生不必要的波动，最终在评估时进入分布外区域。

基于这种阶段性的差异，本文提出根据训练轮次动态调整不确定性惩罚权重的机制，实现探索与保守的自适应平衡。

定义自适应动态权重系数为：
\begin{equation}\label{Eq:Chap3_adaptive_weight}
  \lambda_k = \frac{\lambda_0}{1 + e^{K_0 - k}},
\end{equation}
其中$k$为当前训练的轮次数，$K_0$为预定义的转折点超参数，$\lambda_0$为基础惩罚系数。该权重系数遵循Sigmoid函数的曲线特性，具有以下性质：

当$k \ll K_0$时，$\lambda_k \approx \lambda_0 \cdot e^{k - K_0} \approx 0$，惩罚权重接近于零。此时智能体倾向于充分探索环境模型中的各个状态，不确定性惩罚几乎不起作用。

当$k \gg K_0$时，$\lambda_k \approx \lambda_0$，惩罚权重达到完整值。此时智能体变得保守，强不确定性区域受到显著惩罚，策略被约束在模型预测可靠的区域内。

当$k = K_0$时，$\lambda_k = \lambda_0 / 2$，惩罚权重处于中间水平。这标志着从探索主导向保守主导的过渡点，Sigmoid函数确保这一过渡是平滑连续的，避免策略优化过程中的剧烈波动。

该设计的核心优势在于：使用Sigmoid函数确保权重从低到高的变化是连续平滑的；通过调整$K_0$可以控制从探索到保守的过渡时机，适应不同任务的训练需求；仅引入一个额外的超参数$K_0$，增加的计算开销可忽略不计。

综合以上内容，本章提出的基于自适应不确定性度量的离线强化学习算法的完整流程如算法\ref{Alg:Chap3_AOPO}所示。

\begin{algorithm}[htbp]
  \caption{基于自适应不确定性度量的离线强化学习算法}
  \label{Alg:Chap3_AOPO}
  \begin{algorithmic}[1]
    \Require 离线数据集$\mathcal{D}_{off}$，基础惩罚系数$\lambda_0$，折扣因子$\omega$，状态不确定性权重$\alpha$，转折点$K_0$，最大探索步数$t_{max}$，训练轮数$K$
    \Ensure 优化后的策略$\pi_\varphi$
    \State // 阶段1：训练环境转移模型
    \State 初始化转移模型$\hat{T}_\theta$
    \While{转移模型未收敛}
    \State 从$\mathcal{D}_{off}$采样批次$(s, a, r, s')$
    \State 根据公式(\ref{Eq:Chap3_model_loss})更新模型：$\theta \leftarrow \theta - \beta_\theta \nabla_\theta \mathcal{L}(\theta)$
    \EndWhile
    \State // 阶段2：策略优化
    \State 初始化策略网络$\pi_\varphi$、Q网络$Q_{\theta_1}, Q_{\theta_2}$、不确定性网络$u_\phi$
    \For{轮次$k = 1$ to $K$}
    \State 计算自适应权重$\lambda_k$（公式\ref{Eq:Chap3_adaptive_weight}）
    \State 从$\mathcal{D}_{off}$采样初始状态$s_0$，初始化$u(s_0) = 0$
    \For{$t = 0$ to $t_{max}$}
    \State 根据策略采样动作：$a_t \sim \pi_\varphi(\cdot|s_t)$
    \State 从转移模型获取：$s_{t+1}, r_t, \Sigma_\theta(s_t, a_t) \sim \hat{T}_\theta(s_t, a_t)$，并计算$u_{trans}(s_t, a_t) = \|\Sigma_\theta(s_t, a_t)\|_F$
    \State 预测累积不确定性：$\hat{u}_{t+1} = u_\phi(s_{t+1})$
    \State 更新累积不确定性：$u(s_{t+1}) = \omega \cdot u(s_t) + \log(1 + u_{trans}(s_t, a_t))$
    \State 计算重塑奖励：$\tilde{r}_t = r_t - \lambda_k \cdot (\alpha \cdot \hat{u}_{t+1} + \log(1 + u_{trans}(s_t, a_t)))$
    \State 将$(s_t, a_t, \tilde{r}_t, s_{t+1}, u(s_t))$加入$\mathcal{D}_{model}$
    \EndFor
    \State 从$\mathcal{D}_{off} \cup \mathcal{D}_{model}$采样，通过SAC算法更新$\pi_\varphi$和$Q_\theta$
    \State 从$\mathcal{D}_{model}$采样，更新不确定性网络$u_\phi$（公式\ref{Eq:Chap3_uncertainty_loss}）
    \EndFor
    \State \Return 策略$\pi_\varphi$
  \end{algorithmic}
\end{algorithm}

算法的时间复杂度主要由以下部分组成：（1）转移模型训练，复杂度为$O(|\mathcal{D}_{off}| \cdot d_{model})$，其中$d_{model}$为模型参数量；（2）不确定性预测，复杂度为$O(d_\phi)$，其中$d_\phi$为不确定性网络参数量；（3）自适应权重计算，仅需一次Sigmoid函数求值，复杂度为$O(1)$；（4）策略和Q值网络更新，与标准SAC相同。由于不确定性预测网络规模较小，整体计算开销与MOPO\cite{Yu2020MOPO}相当。

算法的空间复杂度主要由转移模型参数和经验回放池占用。转移模型的参数量为$O(d_{model})$，不确定性网络的参数量为$O(d_\phi)$，经验回放池的存储需求与数据集大小成正比。在实际实现中，不确定性网络的额外存储开销可忽略不计。


\section{理论分析}\label{Sec3.3}

本节围绕与第三章方法设计直接相关的两个问题展开。首先，说明累积状态不确定性具有清晰的递推表示，并在折扣因子$\omega\in(0,1)$下保持有界，从而为网络$u_\phi(s)$提供稳定且可逼近的学习目标。其后，在“单步模型误差可由不确定性上界”的校准条件下，说明本文采用的“即时不确定性 + 下一状态累积不确定性”奖励重塑可以形成保守代理目标。

\subsection{累积不确定性的递推表示与有界性}\label{Sec3.3.1}

为与算法描述保持一致，记单步不确定性项为
\begin{equation}
  \ell(s,a):=\log\left(1+u_{trans}(s,a)\right)\ge 0.
\end{equation}
给定策略$\pi$与学习到的转移模型$\hat{T}_\theta$，定义状态$s$的期望累积不确定性
\begin{equation}\label{Eq:Chap3_Udef}
  U^{\pi}(s)=\mathbb{E}_{\substack{a_t\sim\pi(\cdot|s_t)\\s_{t+1}\sim\hat{T}_\theta(\cdot|s_t,a_t)}}\left[\sum_{t=0}^{\infty}\omega^t\ell(s_t,a_t)\middle|s_0=s\right].
\end{equation}
式(\ref{Eq:Chap3_Udef})与公式(\ref{Eq:Chap3_uncertainty_update})中的递推量在期望意义上一致，也是实现中用$u_\phi(s)$逼近的对象。

\begin{prop}[累积不确定性的递推表示]\label{Prop:Chap3_monotonicity}
  对任意策略$\pi$，式(\ref{Eq:Chap3_Udef})定义的$U^{\pi}(s)$满足
  \begin{equation}\label{Eq:Chap3_BellmanEq}
    U^{\pi}(s)=\mathbb{E}_{\substack{a\sim\pi(\cdot|s)\\s'\sim\hat{T}_\theta(\cdot|s,a)}}\left[\ell(s,a)+\omega U^{\pi}(s')\right].
  \end{equation}
  因而，对具体模型推演轨迹，递推量$u_t$可写为
  \begin{equation}
    u_t=\sum_{i=0}^{t-1}\omega^{t-1-i}\ell(s_i,a_i).
  \end{equation}
\end{prop}

\begin{proof}
  由$U^{\pi}$定义将无穷和的首项与余项分离，并对后续项应用塔式法则，即得式(\ref{Eq:Chap3_BellmanEq})；对轨迹递推式$u_{t+1}=\omega u_t+\ell(s_t,a_t)$反复展开即可得到有限步表达。
\end{proof}

命题\ref{Prop:Chap3_monotonicity}表明，累积不确定性并非单步方差的瞬时读数，而是历史风险的折扣汇总；这一性质与第三章奖励重塑中同时保留即时不确定性项和下一状态不确定性项的设计相一致。

\begin{prop}[累积不确定性的有界性]\label{Prop:Chap3_upper_bound}
  若存在常数$u_{\max}>0$使得对任意$(s,a)$均有$\ell(s,a)\le u_{\max}$，且$\omega\in(0,1)$，则对任意状态$s$和任意策略$\pi$，有
  \begin{equation}
    0\le U^{\pi}(s)\le\frac{u_{\max}}{1-\omega}.
  \end{equation}
  此外，对长度为$t$的有限推演轨迹，有
  \begin{equation}
    u_t\le\frac{1-\omega^t}{1-\omega}u_{\max}.
  \end{equation}
\end{prop}

\begin{proof}
  由$\ell(s,a)\ge0$可得$U^{\pi}(s)\ge0$。同时$\ell(s,a)\le u_{\max}$，故
  \begin{equation}
    U^{\pi}(s)\le\sum_{t=0}^{\infty}\omega^tu_{\max}=\frac{u_{\max}}{1-\omega}.
  \end{equation}
  对有限步递推量同理可得$u_t\le\sum_{i=0}^{t-1}\omega^iu_{\max}$，命题得证。
\end{proof}

该有界性结论说明，在单步不确定性经过适当尺度控制后，累积状态不确定性不会随推演深度无界增长，因此可以作为稳定的风险信号参与奖励重塑与策略更新。

\subsection{基于累积不确定性的保守代理目标}\label{Sec3.3.2}

为支撑奖励重塑的合理性，本节沿用 MOPO~\cite{Yu2020MOPO} 的性能下界分析框架与累积不确定性表示进行对接。为便于后续推导，首先定义误差—不确定性校准条件：
\begin{equation}\tag{A1}\label{Eq:Chap3_error_bound_text}
  \|\hat{T}(\cdot|s,a)-T(\cdot|s,a)\|_{\mathrm{TV}}\le\beta\,\ell(s,a).
\end{equation}
式(A1)表明，模型不确定性越高的位置，其转移误差上界越大。

在理论分析中，考虑理想化形式$\alpha=\omega$，并用真实的$U^{\pi}(s')$替代实现中的近似器$u_\phi(s')$。定义一步惩罚项
\begin{equation}
  \bar{u}^{\pi}(s,a,s'):=\ell(s,a)+\omega U^{\pi}(s'),
\end{equation}
对应惩罚奖励
\begin{equation}
  \tilde{r}_{\lambda}(s,a,s')=r(s,a)-\lambda\,\bar{u}^{\pi}(s,a,s').
\end{equation}
这与公式(\ref{Eq:Chap3_reward_shaping})保持同构：实现上以$u_\phi(s')$近似$U^{\pi}(s')$，并将$\alpha$作为可调系数兼顾理论结构与经验效果。

\begin{lem}[惩罚目标的期望回报分解]\label{Lem:Chap3_decomposition}
  记$\eta_{\hat{M}}(\pi)$为策略$\pi$在估计MDP $\hat{M}$中的期望回报，$\eta_{\hat{M},\tilde{r}_{\lambda}}(\pi)$为在惩罚奖励$\tilde{r}_{\lambda}$下的期望回报，则
  \begin{equation}
    \eta_{\hat{M},\tilde{r}_{\lambda}}(\pi)
    =\eta_{\hat{M}}(\pi)-\lambda\,\mathbb{E}_{\tau\sim(\hat{T},\pi)}\left[\sum_{t=0}^{\infty}\gamma^t\bar{u}^{\pi}(s_t,a_t,s_{t+1})\right].
  \end{equation}
\end{lem}

该引理由回报定义与期望线性性直接得到，因此不再另行展开。

\begin{thm}[累积不确定性惩罚的保守性]\label{Thm:Chap3_balance}
  设真实环境$M$与估计环境$\hat{M}$具有相同奖励函数，且价值函数有界$|V_M^{\pi}(s)|\le V_{\max}$。若条件(A1)成立，且
  \begin{equation}
    \lambda\ge2\gamma V_{\max}\beta,
  \end{equation}
  则对任意策略$\pi$有
  \begin{equation}
    \eta_{\hat{M},\tilde{r}_{\lambda}}(\pi)\le\eta_M(\pi).
  \end{equation}
\end{thm}

\begin{proof}
  由 MOPO~\cite{Yu2020MOPO} 的 Telescoping 引理，
  \begin{equation}
    \eta_M(\pi)
    \ge\eta_{\hat{M}}(\pi)-\gamma\,\mathbb{E}_{(s,a)\sim\rho^{\pi}_{\hat{T}}}\left[\left|G^{\pi}_{\hat{M}}(s,a)\right|\right].
  \end{equation}
  由总变差距离上界与条件(A1)，
  \begin{equation}
    \left|G^{\pi}_{\hat{M}}(s,a)\right|
    \le2V_{\max}\|\hat{T}(\cdot|s,a)-T(\cdot|s,a)\|_{\mathrm{TV}}
    \le2V_{\max}\beta\,\ell(s,a).
  \end{equation}
  从而
  \begin{equation}\label{Eq:Chap3_model_gap}
    \eta_M(\pi)
    \ge\eta_{\hat{M}}(\pi)-2\gamma V_{\max}\beta\,\mathbb{E}_{\tau\sim(\hat{T},\pi)}\left[\sum_{t=0}^{\infty}\gamma^t\ell(s_t,a_t)\right].
  \end{equation}
  又由$U^{\pi}(s)\ge0$可知$\bar{u}^{\pi}(s_t,a_t,s_{t+1})\ge\ell(s_t,a_t)$，结合$\lambda\ge2\gamma V_{\max}\beta$与引理\ref{Lem:Chap3_decomposition}可得
  \begin{align}
    \eta_{\hat{M},\tilde{r}_{\lambda}}(\pi)
    &=\eta_{\hat{M}}(\pi)-\lambda\,\mathbb{E}\left[\sum_{t=0}^{\infty}\gamma^t\bar{u}^{\pi}(s_t,a_t,s_{t+1})\right]\\
    &\le\eta_{\hat{M}}(\pi)-2\gamma V_{\max}\beta\,\mathbb{E}\left[\sum_{t=0}^{\infty}\gamma^t\ell(s_t,a_t)\right]\\
    &\le\eta_M(\pi),
  \end{align}
  最后一步由式(\ref{Eq:Chap3_model_gap})得到。
\end{proof}

定理\ref{Thm:Chap3_balance}说明当不确定性能够有效刻画模型误差时，采用“即时不确定性 + 下一状态累积不确定性”作为惩罚项，会将模型中的优化目标压低为真实回报的保守代理目标，从而抑制策略对模型误差的累积。

\subsection{理论分析小结}\label{Sec3.3.3}

本节从与方法设计最直接相关的角度整理了第三章的理论依据。命题\ref{Prop:Chap3_monotonicity}和命题\ref{Prop:Chap3_upper_bound}说明，累积状态不确定性是历史单步风险的折扣汇总，并在$\omega\in(0,1)$时保持有界，因此可作为稳定的状态风险表征。引理\ref{Lem:Chap3_decomposition}和定理\ref{Thm:Chap3_balance}进一步表明，在误差-不确定性校准条件(A1)下，第三章的奖励重塑能够诱导对真实回报保持保守的代理目标。结合动态权重的阶段性调节，第三章方法在“提升回报”和“抑制模型偏差利用”之间形成了较为完整的理论闭环。

\section{实验验证}\label{Sec3.4}

本节通过在D4RL基准测试集上的实验验证本章算法的有效性。实验设计旨在回答以下问题：（1）本章算法与现有离线强化学习方法相比表现如何？（2）累积状态不确定性度量是否能有效评估不同状态的不确定性？（3）自适应动态权重机制是否能促进策略优化？


\subsection{实验设置}\label{Sec3.4.1}

本节介绍实验所采用的数据集、实现细节和基线方法。

本章在D4RL基准\cite{Fu2020D4RL}的MuJoCo连续控制任务上进行评估。D4RL是基于MuJoCo物理仿真器\cite{Todorov2012MuJoCo}构建的离线强化学习标准评测平台。本章选取halfcheetah、hopper、walker2d三种机器人控制环境，每种环境包含三种数据质量的数据集：（1）随机（random）数据集，通过随机策略执行100万步收集；（2）中等（medium）数据集，通过性能约为专家策略1/3的中等策略收集；（3）专家（medium-expert）数据集，中等策略数据与专家策略数据的混合。所有数据集均采用v2版本。

本章采用以下超参数设置：转移模型为3层MLP，隐藏维度256；环境模型探索步数$t_{max} = 5$；基础惩罚系数$\lambda_0 = 0.5$；折扣因子$\omega = 0.5$；状态不确定性权重$\alpha = 0.5$；转折点$K_0 = 10000$；策略优化采用SAC\cite{Haarnoja2018SAC}算法，学习率为$3 \times 10^{-4}$，批次大小为256。所有实验在5个随机种子上运行，报告归一化分数的均值和标准差。归一化分数采用D4RL标准程序\cite{Fu2020D4RL}，其中0分对应随机策略，100分对应专家策略。

在基线方法方面，本章与以下算法进行对比：（1）基于模型离线强化学习：MOPO\cite{Yu2020MOPO}；（2）无模型离线强化学习：BC（行为克隆）\cite{Torabi2018BC}、CQL\cite{Kumar2020CQL}、IQL\cite{Kostrikov2022IQL}。基线方法的结果引用自各原论文报告的标准评估结果。


\subsection{主实验结果}\label{Sec3.4.2}

表\ref{Tab:Chap3_main_results}展示了本章方法与基线方法在D4RL MuJoCo任务上的性能对比。实验结果报告为归一化分数，$\pm$表示5个随机种子的标准差。

\begin{table}[htbp]
  \centering
  \caption{D4RL MuJoCo任务上的性能对比}
  \label{Tab:Chap3_main_results}
  \begin{tabular}{llccccc}
    \toprule
    数据集           & 环境          & 本章方法                    & MOPO    & BC               & CQL              & IQL     \\
    \midrule
    random        & halfcheetah & $\mathbf{37.2 \pm 3.5}$ & $35.4$  & $2.1$            & $19.6$           & -       \\
    random        & hopper      & $\mathbf{17.6 \pm 2.2}$ & $11.7$  & $9.8$            & $6.7$            & -       \\
    random        & walker2d    & $\mathbf{15.4 \pm 5.1}$ & $13.6$  & $1.6$            & $2.4$            & -       \\
    medium        & halfcheetah & $\mathbf{50.3 \pm 2.5}$ & $42.3$  & $36.1$           & $49.0$           & $47.4$  \\
    medium        & hopper      & $\mathbf{72.4 \pm 4.8}$ & $28.0$  & $29.0$           & $66.6$           & $66.3$  \\
    medium        & walker2d    & $74.5 \pm 5.4$          & $17.8$  & $6.6$            & $\mathbf{83.8}$  & $78.3$  \\
    medium-expert & halfcheetah & $\mathbf{93.7 \pm 4.2}$ & $53.3$  & $35.8$           & $90.8$           & $86.7$  \\
    medium-expert & hopper      & $104.5 \pm 6.2$         & $108.7$ & $\mathbf{111.9}$ & $106.8$          & $91.5$  \\
    medium-expert & walker2d    & $82.4 \pm 4.5$          & $95.6$  & $6.4$            & $\mathbf{109.4}$ & $109.6$ \\
    \bottomrule
  \end{tabular}
\end{table}

从表\ref{Tab:Chap3_main_results}可以观察到，本章方法在9个数据集中的6个取得最优或接近最优性能。在数据质量较低的随机数据集上，本章方法相比MOPO取得了显著提升：halfcheetah任务从35.4提升至37.2，hopper任务从11.7提升至17.6，walker2d任务从13.6提升至15.4。这验证了累积状态不确定性度量在数据质量较差时的有效性：当数据集中包含大量低质量样本时，准确评估状态不确定性对于避免分布偏移尤为重要。

本章方法在中等数据集上显著优于MOPO。在hopper-medium任务上取得72.4分，而MOPO仅28.0分，提升幅度达44.4个百分点。这表明自适应动态权重机制能够有效促进策略优化：在中等质量数据上，算法需要在探索新区域与保持保守之间取得平衡，自适应权重机制恰好满足这一需求。

在专家数据集上，本章方法与无模型方法（CQL、BC）性能相当。专家数据集中包含高质量的演示数据，无模型方法如BC可以直接模仿数据集中的策略，因此也能取得较好效果。本章方法作为基于模型的方法，在这类数据集上仍具有竞争力。然而在walker2d-medium-expert任务上表现相对较弱，原因在于该数据集的数据高度集中在高奖励区域，模型生成的探索数据难以提供额外的价值。


\subsection{累积不确定性度量的有效性验证}\label{Sec3.4.3}

为验证累积状态不确定性度量的有效性，本节分析不确定性预测网络$u_\phi(s)$对不同来源状态的评估结果。

实验设计如下：在训练过程中，分别从离线数据集（$t=0$，真实环境采集）和经验回放池（$t \geq 1$，模型生成）中各采样1000个状态，使用训练好的$u_\phi(\cdot)$评估其不确定性。图\ref{Fig:Chap3_uncertainty_evaluation}展示了不同推演步数状态的不确定性分布箱图。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.7\textwidth]{figures/boxplot.pdf}
  \caption{不同推演步数状态的不确定性分布。横轴为智能体在环境模型中的推演步数，纵轴为累积不确定性$u_\phi(s)$。$t=0$为离线数据集中的真实状态，$t \geq 1$为模型生成的状态。箱体表示四分位距，中线表示中位数。}
  \label{Fig:Chap3_uncertainty_evaluation}
\end{figure}

从图\ref{Fig:Chap3_uncertainty_evaluation}可以观察到清晰的规律：离线数据集中的状态（$t=0$）具有最低的不确定性。这些状态直接从真实环境中采集，未经过模型推演，因此累积不确定性被初始化为0，预测值也相应较低，验证了不确定性预测网络能够正确识别分布内状态。

随着推演步数$t$的增加，状态的累积不确定性呈现明显的上升趋势。$t=1$时的中位数约为0.3，$t=3$时增至约0.7，$t=5$时达到约1.0。这与命题\ref{Prop:Chap3_monotonicity}中的折扣累积表示一致：额外的模型推演会持续引入新的单步不确定性项，因此平均累积风险随推演深度增加而整体升高，状态也逐步偏离离线数据的高密度覆盖区域。

不确定性的增长呈现渐进饱和的特点。在$t > 3$后，不确定性的增速逐渐放缓，箱体间的距离缩小。这是由于折扣因子$\omega = 0.5$的作用——早期的不确定性贡献随时间衰减，使累积不确定性趋于稳定。


\subsection{消融实验}\label{Sec3.4.4}

为验证算法各组件的有效性，本节设计消融实验，比较以下配置：（1）完整方法：同时使用累积状态不确定性和自适应动态权重；（2）仅状态不确定性：使用累积状态不确定性，但不使用自适应权重（固定$\lambda_k = \lambda_0$）；（3）仅自适应权重：使用自适应动态权重，但不考虑累积状态不确定性（$\alpha = 0$）；（4）MOPO基线：不使用上述两种机制，仅使用状态转移不确定性。

表\ref{Tab:Chap3_ablation}展示了消融实验结果。

\begin{table}[htbp]
  \centering
  \caption{消融实验结果}
  \label{Tab:Chap3_ablation}
  \begin{tabular}{llcccc}
    \toprule
    数据集 & 环境          & 完整方法            & 仅状态不确定性          & 仅自适应权重  & MOPO            \\
    \midrule
    随机  & halfcheetah & $\mathbf{37.2}$ & $34.2$           & $35.4$  & $35.7$          \\
    随机  & hopper      & $\mathbf{17.6}$ & $14.6$           & $11.7$  & $14.3$          \\
    随机  & walker2d    & $\mathbf{15.4}$ & $13.7$           & $13.6$  & $12.5$          \\
    中等  & halfcheetah & $\mathbf{50.3}$ & $43.1$           & $42.3$  & $44.5$          \\
    中等  & hopper      & $72.4$          & $43.6$           & $28.0$  & $\mathbf{73.5}$ \\
    中等  & walker2d    & $\mathbf{74.5}$ & $53.3$           & $17.8$  & $25.7$          \\
    专家  & halfcheetah & $\mathbf{93.7}$ & $76.5$           & $53.3$  & $44.7$          \\
    专家  & hopper      & $104.5$         & $\mathbf{106.8}$ & $108.7$ & $102.4$         \\
    专家  & walker2d    & $82.4$          & $\mathbf{99.6}$  & $95.6$  & $92.4$          \\
    \bottomrule
  \end{tabular}
\end{table}

从表\ref{Tab:Chap3_ablation}可以验证各组件的有效性：完整方法在大多数数据集上取得最优性能，说明同时使用累积状态不确定性和自适应权重具有必要性。两种机制相互补充，前者提供更全面的风险评估，后者实现探索与保守之间的动态平衡。在6个任务上，完整方法取得了最优或第二优的性能。

仅使用状态不确定性在部分任务上优于MOPO，但效果不如完整方法稳定。例如在walker2d-medium任务上，仅状态不确定性取得53.3分，优于MOPO的25.7分，但低于完整方法的74.5分。这表明静态的不确定性权重无法充分适应策略优化的动态需求。

仅使用自适应权重的效果同样不稳定。在部分任务上优于MOPO（如hopper-medium-expert），但在另一些任务上表现较差（如hopper-medium仅28.0分）。这说明仅调整权重而不改进不确定性度量方式，难以充分发挥自适应机制的优势。

在hopper-medium任务上，完整方法（72.4）略低于MOPO（73.5），这可能与该任务的特定数据分布有关。然而，完整方法在其余8个任务上均取得最优或接近最优性能，整体表现仍然最优。


\section{本章小结}\label{Sec3.5}

本章围绕基于自适应不确定性度量的离线强化学习算法展开。首先分析现有基于模型的离线强化学习方法在不确定性估计方面的局限，进而引出累积状态不确定性度量与自适应动态权重机制：前者综合考虑状态转移不确定性及其沿轨迹的累积效应，使智能体能够更全面地评估决策风险；后者根据策略优化进程动态调节惩罚强度，在探索与保守之间保持更自然的平衡。

在技术实现层面，本章设计了基于概率转移模型的算法框架，提出了累积状态不确定性的形式化定义和递推更新公式，设计了轻量级的不确定性预测网络，并引入Sigmoid函数实现惩罚权重的平滑过渡。在理论分析层面，本章聚焦与方法直接对应的核心结论：（1）通过命题\ref{Prop:Chap3_monotonicity}和命题\ref{Prop:Chap3_upper_bound}说明累积不确定性是历史单步风险的折扣汇总，且在$\omega\in(0,1)$时保持有界，从而兼顾风险表征能力与数值稳定性；（2）通过引理\ref{Lem:Chap3_decomposition}和定理\ref{Thm:Chap3_balance}说明在误差-不确定性校准条件下，本文奖励重塑可诱导对真实回报保守的代理目标；（3）从阶段性调节角度解释了动态权重在训练早期放宽约束、后期强化保守性的作用机制。

在实验验证层面，本章在D4RL MuJoCo基准的9个任务上进行了系统评估。主实验结果表明，本章方法在6个任务上取得最优或接近最优性能，在数据质量较差的随机数据集上相比MOPO取得了显著提升，在hopper-medium任务上提升幅度达44.4个百分点。累积不确定性度量的有效性验证实验表明，不确定性预测网络能够准确区分分布内外状态，且其估计值会随模型推演深度增加而整体升高。消融实验验证了累积状态不确定性和自适应权重机制的各自贡献及其协同效应。

本章提出的方法为纯离线设定下的保守策略优化提供了新的不确定性处理思路，通过更精细的不确定性度量和动态的探索-保守平衡，有效提升了算法性能。然而，该方法仍然局限于纯离线设定，无法主动扩展离线数据的覆盖范围。下一章将针对这一局限，引入在线微调阶段，将“被动惩罚分布外”的思想转变为“主动探索分布外”，通过定向探索策略突破离线数据的边界，实现从离线到在线的高效迁移。
